# Python Patterns
- Protocol (duck typing) via `typing.Protocol` for interfaces
- `@dataclass(frozen=True)` for immutable DTOs
- Context managers (`with`) for resources (DB connections, file handles)
- Generators for lazy iteration over large datasets
- SQLite with WAL mode + thread-local connections for concurrent access
- References: `python-patterns` skill